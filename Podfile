# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
# set LOCAL_TAR_PATH to the location of your local TruexAdRenderer-tvOS repo (make sure it's set to the right branch)
LOCAL_TAR_PATH = '../TruexAdRenderer-iOS'

# source 'https://github.com/socialvibe/cocoapod-specs.git'
source 'https://github.com/socialvibe/cocoapod-specs-dev.git'

target 'truex-ios-mobile-reference-app' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for truex-ios-mobile-reference-app
  # pod 'TruexAdRenderer', :path => "#{LOCAL_TAR_PATH}"
  # pod 'TruexAdRenderer-iOS', :path => "#{LOCAL_TAR_PATH}"
  pod 'TruexAdRenderer-iOS', '3.2.2'

  target 'truex-ios-mobile-reference-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'truex-ios-mobile-reference-appUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
